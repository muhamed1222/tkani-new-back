# Инструкция по проверке интеграции фронтенда и бэкенда

## Шаг 1: Убедитесь, что бэкенд запущен

В терминале бэкенда проверьте, что сервер работает:

```bash
# В директории myshop-backend
python3 app.py
```

Сервер должен запуститься на `http://localhost:5001`

**Проверка:**
```bash
curl 'http://localhost:5001/api/works?page=1&limit=12'
```

Должен вернуться JSON с данными о работах.

---

## Шаг 2: Убедитесь, что есть тестовые данные

Если база пустая, добавьте тестовые данные:

```bash
# В директории myshop-backend
python3 seed_works.py
```

---

## Шаг 3: Запустите фронтенд

В другом терминале запустите фронтенд:

```bash
# В директории tkani
npm run dev
```

Фронтенд должен запуститься на `http://localhost:5173`

---

## Шаг 4: Откройте страницу работ

В браузере откройте:
```
http://localhost:5173/our_works
```

---

## Шаг 5: Проверьте в DevTools

### A. Вкладка Console (F12 → Console)

**Что должно быть:**
- ✅ Нет ошибок CORS
- ✅ Нет ошибок подключения к API
- ✅ Если есть предупреждения о моковых данных - это нормально (они используются как fallback)

**Что НЕ должно быть:**
- ❌ `Failed to fetch`
- ❌ `CORS policy` ошибки
- ❌ `Network Error`

### B. Вкладка Network (F12 → Network)

1. Обновите страницу (F5)
2. Найдите запрос к `/api/works`
3. Проверьте:
   - **Status**: должен быть `200 OK`
   - **Request URL**: `http://localhost:5001/api/works?page=1&limit=12`
   - **Response**: должен содержать JSON с полями `works`, `total`, `page`, `totalPages`

**Пример правильного ответа:**
```json
{
  "works": [
    {
      "id": 15,
      "title": "Платье из бархата \"Зимняя коллекция\"",
      "image": "/uploads/works/work15.jpg",
      "link": "/work/15"
    },
    ...
  ],
  "total": 15,
  "page": 1,
  "totalPages": 2
}
```

---

## Шаг 6: Визуальная проверка

На странице должны отображаться:

1. ✅ Заголовок "Работы из наших тканей"
2. ✅ Количество товаров (например, "15 товаров")
3. ✅ Сетка с карточками работ (4 колонки на больших экранах)
4. ✅ Пагинация внизу (если работ больше 12)
5. ✅ Кнопки "Назад" и "Вперед" для навигации

---

## Возможные проблемы и решения

### Проблема 1: CORS ошибка

**Ошибка в консоли:**
```
Access to fetch at 'http://localhost:5001/api/works' from origin 'http://localhost:5173' has been blocked by CORS policy
```

**Решение:**
Убедитесь, что в `app.py` настроен CORS:
```python
CORS(app, supports_credentials=True)
```

Перезапустите бэкенд сервер.

---

### Проблема 2: 404 Not Found

**Ошибка:**
```
GET http://localhost:5001/api/works 404 (Not Found)
```

**Решение:**
1. Проверьте, что сервер запущен на порту 5001
2. Проверьте, что Blueprint зарегистрирован в `app.py`:
   ```python
   from routes.works import works_bp
   app.register_blueprint(works_bp, url_prefix="/api")
   ```

---

### Проблема 3: Пустой массив работ

**Причина:** База данных пустая

**Решение:**
```bash
python3 seed_works.py
```

---

### Проблема 4: Отображаются моковые данные вместо реальных

**Причина:** API недоступен или возвращает ошибку

**Решение:**
1. Проверьте, что бэкенд запущен
2. Проверьте URL в `api.js` (должен быть `http://localhost:5001/api`)
3. Проверьте консоль браузера на ошибки
4. Проверьте Network tab - какой статус у запроса?

---

### Проблема 5: Неправильный порт

**Если ваш бэкенд на другом порту:**

1. Создайте файл `.env` в директории `tkani`:
   ```
   VITE_API_URL=http://localhost:ВАШ_ПОРТ/api
   ```

2. Или измените порт в `src/http/api.js`:
   ```javascript
   const API_URL = import.meta.env.VITE_API_URL || 'http://localhost:ВАШ_ПОРТ/api';
   ```

3. Перезапустите фронтенд (`npm run dev`)

---

## Быстрая проверка через curl

Проверьте, что API работает напрямую:

```bash
# Проверка базового запроса
curl 'http://localhost:5001/api/works?page=1&limit=12'

# Проверка пагинации
curl 'http://localhost:5001/api/works?page=2&limit=12'
```

---

## Автоматическая проверка

Запустите тестовый скрипт:

```bash
# В директории myshop-backend
python3 test_api.py
```

Все тесты должны пройти успешно.

---

## Готово! ✅

Если все работает:
- ✅ API возвращает данные
- ✅ Фронтенд отображает работы
- ✅ Пагинация работает
- ✅ Нет ошибок в консоли

Поздравляем! Интеграция работает корректно! 🎉

